{
  "name": "churchsnap-firebase-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Emulator-backed release-readiness coverage for ChurchSnap Firebase Security Rules.",
  "scripts": {
    "test": "node --test release_readiness.rules.test.cjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0"
  }
}
